# 未着手タスク一覧

実装済みの機能をベースに、残っている課題・未実装機能をまとめたもの。

---

## 優先度: 高（本番稼働前に必須）

### [ ] Resend 送信元メールアドレスの変更
- 現在 `src/lib/email.ts` の From が `reservation@example.com`（仮）
- Resendで検証済みドメインを設定し、実際のアドレスに変更する
- 件名・本文のサービス名も実際のものに合わせる

### [ ] RLS ポリシーの設定
- 現在は全テーブルでRLS有効だがポリシー未定義
- バックエンドはanon keyでアクセスしているため実質RLSが効いていない
- サービスロールキー（`SUPABASE_SERVICE_ROLE_KEY`）に切り替えるか、anon key用のポリシーを適切に設定する

### [ ] Magic Linkのシングルユース強制
- `token_used` フラグはDBに存在するが、MyPage表示・finalize時に `token_used = true` への更新処理が未実装
- 使用済みトークンでの再アクセスを弾く処理を追加する

### [ ] 本番環境変数の設定
- Cloudflare Dashboard に全環境変数を設定する
- `MAGIC_LINK_BASE_URL` を本番ドメインに変更する

### [ ] Supabase管理者ユーザーのセットアップ手順書
- 現在 `ADMIN_SECRET` は定義されているが未使用
- 管理者ユーザーをSupabase Authで作成する手順をREADMEまたは別ドキュメントに記載する

---

## 優先度: 中（UX改善・機能追加）

### [ ] ブラックリスト管理UI（管理画面）
- `blacklist` テーブルはDBに存在するが、管理画面からの追加・削除UIが未実装
- 管理ダッシュボードにブラックリスト管理ページを追加する

### [ ] 管理者ノートの入力UI
- `bookings.admin_note` カラムがあるが、管理画面から記入するUIがない
- 承認・却下時のメモ欄として実装する

### [ ] 却下メールの送信
- 現在、却下（reject）時にクライアントへの通知メールが送られない
- 却下理由を添えたメールをResendで送信する

### [ ] Magic Link再送機能
- 管理ダッシュボードに「confirmed」ステータスの予約に対してMagic Linkを再送するボタンはあるが、実際の再送処理が実装されているか確認・実装
- トークンを再生成して新しいリンクを送るフローにする

### [ ] 予約一覧のCSVエクスポート
- 管理者がfinalized予約の一覧をCSVでダウンロードできる機能
- カラム: 予約日時、クライアント名、メール、オプション、最終料金

### [ ] クライアントへの予約完了通知メール
- 現在、仮予約受付時（pending作成時）にクライアントへ自動メールが送られない
- 「仮予約を受け付けました、承認後にメールをお送りします」旨の確認メールを送信する

### [ ] スロット検索の日付バリデーション
- 過去日付・営業時間外の日付を選んでも検索できてしまう
- フロントエンドとバックエンドの両側でバリデーションを追加する

### [ ] 時間帯表示のタイムゾーン明示
- セッション時間の表示に「JST」などタイムゾーンの明示がない箇所がある
- 特に海外クライアントがいる場合に誤解を生む可能性

---

## 優先度: 低（将来的な改善）

### [ ] blocked_datetimes テーブルの活用
- `blocked_datetimes` テーブルがあるが、スロット検索ロジックで参照されていない
- 臨時休業日の設定・管理UIとスロット検索への組み込みを実装する

### [ ] スロット自動延伸の自動化
- 現在はAdmin画面から手動で「+2ヶ月」ボタンを押す必要がある
- Cloudflare Workers Cron Triggers を使って自動的に延伸するジョブを追加する

### [ ] 管理者ダッシュボードのページネーション
- 予約が増えると全件表示で重くなる
- ステータスごとにページネーションを実装する

### [ ] エラーページの整備
- 無効なMagic Link・期限切れトークン・存在しないページへのアクセスに対する、ユーザーフレンドリーなエラーページが未整備

### [ ] ロギング・モニタリング
- Cloudflare Workers のログ確認方法の整備
- エラー発生時のアラート（メールまたはSlack通知）

### [ ] テストの追加
- 現在テストコードが存在しない
- バッファ計算ロジック、トークン検証、スロット空き判定などのユニットテストを追加する

### [ ] 複数カウンセラー対応（拡張）
- 現在はシングルカウンセラー前提の設計
- 将来的に複数カウンセラーが必要になった場合の拡張設計を検討する

---

## 技術的負債・コードのTODO

| ファイル | 内容 |
|---------|------|
| `src/lib/email.ts` | `from` アドレスを環境変数化する |
| `src/admin/index.tsx` | `ADMIN_SECRET` 変数が定義されているが未使用。削除するか用途を明確化 |
| `src/api/finalize.tsx` | `token_used = true` の更新処理を追加する |
| `src/api/search-slot.tsx` | `blocked_datetimes` テーブルを参照するロジックを追加する |
| `supabase/migrations/001_initial_schema.sql` | RLSポリシーを追加する |
